# Build settings
markdown: kramdown

kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge

header_pages:
  - README.md
  - setup.md
  - documentation.md

# The default date for minima, used if none is specified in the tag.
minima:
  date_format: "%b %-d, %Y"

theme: minima
# remote_theme: pages-themes/minimal@v0.2.0